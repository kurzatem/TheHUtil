namespace TheHUtil
{
    using System;

    public class IndexedTuple
    {
        // This is an idea that I had to create an expandable tuple object that is runtime type safe.
        // This is modeled after the dynamic tuple generator built into the eiffel compiler.
        // Access would be like a jagged array. The outer would be by type. The inner would be by integer.
    }
}
